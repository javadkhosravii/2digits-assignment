query SearchBlogs($search: String!) {
  Blogs(where: { _search: $search }) {
    items {
      _id
      title
      _slug
      banner_image {
        url(width: 3000)
      }
      categories {
        _id
        body
        slug
      }
    }
    total
  }
}
